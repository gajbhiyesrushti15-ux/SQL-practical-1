# SQL-practical-1
CREATE TABLE BE (
    Student_id INT PRIMARY KEY,
    name VARCHAR(20),
    department VARCHAR(20),
    age INT
);
INSERT INTO BE
VALUES (1, 'Rahul', 'EC', 22),
       (2, 'Kammo', 'EC', 23);
SELECT *FROM BE;
